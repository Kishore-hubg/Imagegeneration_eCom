[project]
name = "app"
version = "0.1.0"
dependencies = [ "fastapi>=0.115.0", "uvicorn[standard]>=0.32.0", "pillow>=10.0.0", "httpx>=0.27.0", "python-dotenv>=1.0.0", "pydantic>=2.0.0", "pydantic-settings>=2.0.0" ]
requires-python = "~=3.14.0"
